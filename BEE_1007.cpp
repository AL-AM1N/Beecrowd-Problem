#include<iostream>
using namespace std;
int main(){
    int A, B, C, D, DIFERENCA;
    cin>>  A >> B >> C >> D;

    DIFERENCA = (A*B)-(C*D);
    
    printf("DIFERENCA = %d\n",DIFERENCA);
    return 0;
}
